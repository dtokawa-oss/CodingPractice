# Design Doc: GETログイン処理の全廃とQRコードログインのワンタイムトークン化

**作成者:** [あなたの氏名]
**日付:** 2026-09-03
**ステータス:** Draft
**対応する Issue:** [セキュリティ改修] GETリクエストにおける資格情報処理の撤廃および認証方式の刷新

---

## 背景 — Context

`LoginController` の `[HttpGet]` アクションにおいて、URLパラメータ経由でパスワードを受信・復号してログインを実行する実装が存在する。スマホカメラによるQRコード読み取りはWebの仕様上100% `GET` 通信となるため、現状の実装ではアクセスログやブラウザ履歴への資格情報漏洩、およびリプレイ攻撃（暗号文の再利用による不正ログイン）のリスクがある。これらを根本解決するため、認証フロー全体の再設計を行う。

---

## Goals / Non-Goals — 設計観点

### Goals

- `[HttpGet] Login` アクションからの自動ログイン処理の完全除去（画面表示のみに専念させる）
- QRコード読み取りによる自動ログインを、パスワード送信ではなく「使い捨てワンタイムトークン方式（GET）」へ移行する
- フォームからの通常のID/PWログインは、HTTPボディを用いた `POST` 通信に一本化する

### Non-Goals

- ログイン画面のUI/デザイン刷新
- SSO（CNW / Entra ID）連携基盤側の認証ロジックの変更

---

## 設計概要 — Design Overview

**改修後の全体フロー:**

```
【通常ログイン】
  [ユーザー] ──(GET /Login)──> ログイン画面表示(HTML)のみ返却
  [ユーザー] ──(POST /Login)─> ID/PWをHTTPボディで安全に送信・認証

【QRコード自動ログイン】
  1. [事前発行] トークン生成APIで使い捨てトークン(UUID等)を発行しDB/Cacheに保存(有効期限: 5分)
  2. [QR読み取り] スマホカメラ等から GET /Login/Qr?token={token} にアクセス
  3. [検証/処理] サーバーはトークンを検証
     - 有効     : セッションを確立、トークンを即時破棄（使用済化）してマイページへ転送
     - 無効/期限切れ: エラーメッセージを表示して通常のログイン画面(GET /Login)へ誘導
```

---

## 詳細設計 — Detailed Design

- **`[HttpGet] Login` の単一責任化**
  `if (merchantCode != null && loginId != null && password != null)` の判定ブロックを完全削除し、GETアクセス時はログイン画面（HTML）の返却のみを行う。

- **専用アクション `[HttpGet] LoginByToken` の新設**
  QRコード専用のGETエンドポイント（`/Login/Qr`）を新たに作成し、引数として `token` のみを受け取る。

- **ワンタイムトークンの設計**
  - `System.Security.Cryptography.RandomNumberGenerator` を用い、予測不可能な文字列を生成する。
  - 発行後「5分間有効」かつ「1回使用した瞬間に即時削除/無効化」とする（単一使用性の徹底）。

- **クレーム設定の修正**
  `ClaimRoleSet` メソッド内で `ClaimTypes.Name` が2重登録されていた実装を修正し、`MerchantCode` はカスタムクレームとして保持する。

---

## 代替案と不採用理由 — Alternatives Considered

| 代替案 | 不採用理由 |
| --- | --- |
| QRコードアクセス時もPOST通信に強制する | スマホの標準カメラアプリから標準ブラウザでURLを開く動作はWeb標準の仕様上100% `GET` リクエストとなるため、技術的に実現不可能。 |
| GET通信のままパスワードの暗号化強度を上げる | 暗号文であってもURL（アクセスログや閲覧履歴）に残留するため、その暗号文をコピー＆ペーストされるだけでログインできてしまう（リプレイ攻撃を防げない）ため。 |

---

## セキュリティ・リスク

- **トークン盗聴リスク** — ネットワーク通信上の盗聴を防ぐため、サイト全域で HTTPS 通信（HSTS指定）を必須とする。
- **トークンの総当たり（ブルートフォース）攻撃** — トークン桁数を十分確保（UUID v4レベル）し、有効期限を極小（5分）に設定することで総当たりを不可能にする。

---

## テスト・ロールアウト計画

### 検証

- 旧仕様のURL（`?password=...` 付き）でアクセスした場合に自動ログインが作動せず、安全に通常のログイン画面が表示されることを確認する単体テストを追加する。
- ワンタイムトークンが「1度使用された後」「5分経過後」に失効し、再ログインできないことを確認する統合テストを実施する。

### ロールアウト

1. トークン発行・検証用API（`/Login/Qr`）をデプロイ
2. QRコード生成システム側の発行先URLを新形式（`token=`）に更新
3. 旧仕様のコード（`LoginController` 内のGET自動ログイン処理）を削除・デプロイ

### 切り戻し

リバートコミットによる旧環境への即時復元。

---

## Open Questions

- 印刷済みの旧QRコード（パスワード埋め込み型URL）が市場に大量に残っている場合、完全にアクセスを拒否してエラー画面に飛ばす運用で問題ないか（顧客への周知計画が必要か）。
- トークンの保持先は既存データベース（RDB）の専用テーブルとするか、応答速度と自動失効制御に優れた Redis 等のインメモリキャッシュを使用するか。

---

## AI メモ

- **受け取ったインプット:** `LoginController.cs` のソースコード、および「GETでのパスワード受信廃止」と「QRコード読み取り＝GET通信仕様」に関する議論成果。
- **置いた仮定:** QRコード生成側システム（URLを発行する側）のコード改修が同時に実施可能であること。
- **未確認事項:** 印刷済みQRコード等の移行期間（後方互換性）の考慮が必要かどうか。
